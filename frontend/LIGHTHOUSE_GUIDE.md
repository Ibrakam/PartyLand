# 🚀 Lighthouse Performance Check Guide

## Вариант 1: Автоматический запуск (Рекомендуется)

### Шаг 1: Запустите dev сервер
```bash
cd frontend
npm run dev
```

Сервер должен запуститься на `http://localhost:3000`

### Шаг 2: В новом терминале запустите Lighthouse
```bash
cd frontend
npm run lighthouse
```

Это проверит обе страницы (главную и товары).

### Или проверить отдельные страницы:
```bash
# Главная страница
npm run lighthouse:home

# Страница товаров
npm run lighthouse:products
```

### Результаты
Отчеты будут сохранены в папке `frontend/lighthouse/`:
- `home.html` - отчет для главной страницы
- `products.html` - отчет для страницы товаров

Откройте HTML файлы в браузере для просмотра детальных отчетов.

---

## Вариант 2: Через браузер Chrome (Самый простой)

### Шаг 1: Запустите dev сервер
```bash
cd frontend
npm run dev
```

### Шаг 2: Откройте Chrome DevTools
1. Откройте `http://localhost:3000` в Chrome
2. Нажмите `F12` или `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
3. Перейдите на вкладку **"Lighthouse"**
4. Выберите категории для проверки:
   - ✅ Performance
   - ✅ Accessibility
   - ✅ Best Practices
   - ✅ SEO
5. Выберите **Mobile** или **Desktop**
6. Нажмите **"Generate report"**

### Результат
Lighthouse автоматически создаст отчет и покажет метрики:
- Performance Score (0-100)
- Accessibility Score (0-100)
- Best Practices Score (0-100)
- SEO Score (0-100)

---

## Вариант 3: Через командную строку (npx)

### Шаг 1: Запустите dev сервер
```bash
cd frontend
npm run dev
```

### Шаг 2: В новом терминале запустите Lighthouse
```bash
# Проверить главную страницу
npx lighthouse http://localhost:3000 \
  --output=html \
  --output-path=./lighthouse/home.html \
  --only-categories=performance,accessibility,best-practices,seo

# Проверить страницу товаров
npx lighthouse http://localhost:3000/products \
  --output=html \
  --output-path=./lighthouse/products.html \
  --only-categories=performance,accessibility,best-practices,seo
```

---

## 📊 Целевые метрики

После внесенных улучшений ожидаемые результаты:

- **Performance**: ≥ 95
- **Accessibility**: ≥ 100
- **Best Practices**: ≥ 100
- **SEO**: ≥ 100

### Основные метрики Performance (Core Web Vitals):

- **LCP (Largest Contentful Paint)**: < 2.5s
- **FID (First Input Delay)**: < 100ms
- **CLS (Cumulative Layout Shift)**: < 0.1

---

## 🔍 Что проверяет Lighthouse

### Performance
- Скорость загрузки страницы
- Time to Interactive
- First Contentful Paint
- Core Web Vitals (LCP, FID, CLS)

### Accessibility
- ARIA атрибуты
- Контрастность цветов
- Семантические HTML теги
- Доступность с клавиатуры

### Best Practices
- HTTPS
- Современные веб-стандарты
- Отсутствие ошибок консоли
- Безопасность

### SEO
- Мета-теги
- Структурированные данные
- Мобильная адаптивность
- Семантическая разметка

---

## 🛠 Устранение проблем

### Если Lighthouse не запускается:
```bash
# Установите lighthouse глобально
npm install -g lighthouse

# Или используйте npx (рекомендуется)
npx lighthouse --version
```

### Если сервер не отвечает:
Убедитесь, что dev сервер запущен:
```bash
# Проверьте, что порт 3000 занят
lsof -i :3000
# или
curl http://localhost:3000
```

---

## 💡 Советы

1. **Всегда проверяйте production build**:
   ```bash
   npm run build
   npm run start
   # Затем запустите Lighthouse на http://localhost:3000
   ```

2. **Используйте мобильное устройство для тестирования**:
   Lighthouse может симулировать мобильные устройства

3. **Проверяйте регулярно**:
   Запускайте Lighthouse после каждого значимого изменения

4. **Сравнивайте результаты**:
   Сохраняйте отчеты для сравнения до/после оптимизаций

